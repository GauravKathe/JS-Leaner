"use strict"
/* Note :  
1. "use strict" -> strict mode (used for showcasing that we're using strict mode means latest java script (New Standards))

2. alert() -> alert statement can't be used in local machine while executing the js file in node js 
if we want to use aler() we can embeed it into html file or use in browser console 
alert("heyy")
*/

/* 
Data Types in Java Script 
1. number 
2. bigint 
3. string
4. float
5. null
6. undefined [when we dont assign an value to an variable]
*/
let a=6
let user_name = "Gaurav Kathe"
let bool=true
let num=100.1
let x=null;
let cc;
console.table([typeof a,typeof user_name,typeof bool,typeof num,typeof x,typeof cc])

console.table([a,user_name,bool,num])
